Read all Rust source in `control-plane/src` and `cli/src` (41 files total). Not converged: 4 real production bugs.

- High - engineer impersonation via `/start`: the API trusts caller-supplied `req.engineer`, reuses that engineer record if it exists, and the loop later loads that engineer’s stored credentials for pod injection. Any bearer-token holder can start work as another engineer and inherit their credential refs. Refs: `control-plane/src/api/handlers.rs:78`, `control-plane/src/loop_engine/driver.rs:1282`
- High - credential sidecar is not actually mounted: `job_builder` comments say secrets are mounted into a sidecar, but the pod spec creates only one agent container and only mounts the bare repo PVC. Secret names are exposed as env vars, but no sidecar, secret volume, or secret mount exists, so jobs that rely on mounted creds will fail auth. Refs: `control-plane/src/k8s/job_builder.rs:110`, `control-plane/src/k8s/job_builder.rs:163`
- High - `nemo auth` builds invalid Kubernetes Secret names from common engineer names: it uses raw `engineer` in `nemo-creds-{engineer}` with no RFC1123 normalization, while the API rejects uppercase, underscores, spaces, etc. Common names like `Alice` or `john_doe` fail registration. Refs: `cli/src/commands/auth.rs:23`, `cli/src/commands/auth.rs:57`, `control-plane/src/api/handlers.rs:471`
- Medium - first-time `nemo auth` cannot succeed: the CLI registers credentials before the engineer exists server-side, but the API rejects `/credentials` for unknown engineers, and engineer creation only happens during `/start`. Fresh users cannot pre-register credentials as intended. Refs: `cli/src/commands/auth.rs:56`, `control-plane/src/api/handlers.rs:496`, `control-plane/src/api/handlers.rs:78`

If you want, I can do Round 7 with fix proposals only, still without editing.

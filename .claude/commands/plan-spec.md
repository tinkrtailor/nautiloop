---
description: Create or revise an implementation plan for a spec
argument-hint: [spec-path-relative-to-repo]
---

Use the spec-planner subagent to create an implementation plan for the spec at @$1.

Analyze the spec and codebase, then create a detailed plan file at `specs/<spec-name>-impl-plan.md`.

Present the plan for approval before execution begins.
